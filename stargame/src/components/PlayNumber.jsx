import React from "react";

function PlayNumber() {
  return <h2>PlayNumber</h2>;
}

export default PlayNumber;

{
  /* <button
className="number"
style={{backgroundColor: colors[props.status]}}
onClick={() => props.onClick(props.number, props.status)}
>
{props.number}
</button> */
}
